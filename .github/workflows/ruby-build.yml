name: Testing Ruby Build

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Jekyll Build
        uses: actions/jekyll-build-pages@v1

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v1
